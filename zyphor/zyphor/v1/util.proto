edition = "2023";

package zyphor.v1;

import "buf/validate/validate.proto";
import "google/api/annotations.proto";
import "google/protobuf/timestamp.proto";

service UtilService {
  rpc GetVersion(GetVersionRequest) returns (GetVersionResponse) {
    option idempotency_level = NO_SIDE_EFFECTS;
    option (google.api.http) = {get: "/v1/version"};
  }
  rpc ListAuditLogs(ListAuditLogsRequest) returns (ListAuditLogsResponse) {
    option idempotency_level = NO_SIDE_EFFECTS;
    option (google.api.http) = {get: "/v1/audit"};
  }
}

message GetVersionRequest {}
message GetVersionResponse {
  string version = 1;
}

message AuditLog {
  string id = 1;
  string workspace_id = 2;
  string actor_id = 3;
  string actor_name = 4;
  string actor_type = 5;
  string action = 6;
  string resource_type = 7;
  AuditMetadata metadata = 8;
  AuditSource source = 9;
  google.protobuf.Timestamp created_at = 10;
}
message AuditMetadata {
  bool success = 1;
  int64 duration_ms = 2;
  map<string, string> details = 3;
}
message AuditSource {
  string ip_address = 1;
  string user_agent = 2;
  map<string, string> details = 3;
}

// Request message with filters and pagination
message ListAuditLogsRequest {
  string workspace_id = 1 [(buf.validate.field).string.uuid = true];

  // Pagination
  int32 page_size = 2 [(buf.validate.field).int32 = {
    gte: 1
    lte: 100
  }]; // Max 100 items
  string page_token = 3; // Cursor for next page (opaque token)

  // Filters (optional)
  string actor_id = 4 [(buf.validate.field).string.uuid = true];
  string action = 5;
  string resource_type = 6;
  google.protobuf.Timestamp from_time = 7;
  google.protobuf.Timestamp to_time = 8;
}

message ListAuditLogsResponse {
  repeated AuditLog audit_logs = 1;
  string next_page_token = 2; // Empty if no more pages
  int32 total_count = 3; // Optional: approximate count for UI
}
